//Numeric
// var indicates declaring a variable
var watchPrice = 150;
watchPrice = 160; // value of variable can vary

//string
var name = 'Muntasir Tonmoy';

// boolean
var isExpensive = ture;

// we can add two string
// we can do math operation addition subtraction multiplication division and modulu
// we can use short hand like += -=
// if we have to increase or decrease only 1 then we can use increament or decrement operator ++ or --
// we can use parseInt() or parseFloat() to get only integer and float value
// we can use variableName.toFixed(value of how many digit we want to see after decimal point) with parseFloat(); function to see specific number of digit after decimal point.
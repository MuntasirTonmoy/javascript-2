var getJob = true;
var savedMoney = 200000;
//*  use tabout extension to get out from the bracket
if (getJob == true && savedMoney > 100000) //we use && if both of the condition is true.
{
    console.log('marriage done');
}
else {
    console.log('marrige not don');
}
